/**
 * Placeholder route for the centre "New Job" tab. Never actually shown — the
 * tab's custom button (NewJobTabButton) pushes /new-job instead of switching
 * here. Expo Router just needs a file to register the tab slot.
 */
export default function NewJobLauncher() {
  return null;
}
